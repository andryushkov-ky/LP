.shadow {
    display: none;
}

.l-menu--open + .shadow {
    display: block;
    position: fixed;
    top: 0;
    right: 0;
    z-index: 8;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0, .4);
}